{
  "scripts": {
    "format": "prettier --write .",
    "test": "npx playwright test",
    "playwright:test": "playwright test",
    "playwright:ui": "npx playwright test --ui",
    "playwright:snapshot": "npx playwright test --update-snapshots",
    "playwright:report": "playwright test show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2",
    "playwright": "^1.47.2",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
